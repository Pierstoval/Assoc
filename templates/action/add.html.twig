


{% block body %}
<h1>Creation de larticle</h1>
    

    {{ form_start(form) }}

        <div class="form-group">
            {{ form_row(form.title) }}
        </div>

        <div class="form-group row justify-content-between">
            <div class="col-6">
                {{ form_row(form.content) }}
            </div>
            <div class="col-6">
                {{ form_row(form.updtated_at) }}
            </div>
        </div>

        
        <div class="form-group col-6 pl-0">
           {{ form_label(form.adress) }}
           {{ form_widget(form.website) }}
        </div>

        {% if editMode %}
            <div class="form-group col-6 pl-0 ">
                {{ form_row(form.guest) }}
            </div>
        {% else %}
             <div class="form-group col-6 pl-0">
            {{ form_row(form.featured_image) }}
            </div>
        {% endif %}

        <div class="form-group">
            {{ form_row(form.categories) }}
        </div>

           <div class="form-group">
     
        
        {# {% if editMode %}
            <div class="form-group notPrint">
                {{ form_row(form.score, { "value":"0"}) }}
            </div>
        {% else %}
            <div class="form-group notPrint">
                {{ form_row(form.score, { "value":"0"}) }}
            </div>
        {% endif %} #}
{# 
        <div class="form-group notPrint">
            {{ form_row(form.report) }}
        </div> #}
        <div class="flex">


        {% if editMode %}
            <button type ="submit" class=" couleurLavande"> Je mets à jour mon action</button>
        {% else %}
            <button type ="submit" class=" couleurLavande"> J'ajoute mon action </button>
        {% endif %}
        </div>

    {{ form_end(form) }}
    {% endblock %}