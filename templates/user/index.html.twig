{% extends 'user.html.twig' %}

{% block title %}Welcome!
{% endblock %}

{% block body %}


	<div class="grix xs4 container">
		<div class="col-xs4">
			<h1>Profil de {{app.user.firstName}} {{app.user.lastName}}</div>
			</h1>
		<div class="col-xs1">Prenom:</div>
		<div class="col-xs3">{{app.user.firstName}}</div>
		<div class="col-xs1">Nom:</div>
		<div class="col-xs3">{{app.user.lastName}}</div>
		<div class="col-xs1">Email:</div>
		<div class="col-xs3">{{app.user.email}}</div>
		<div class="col-xs4">
			<a href="#" class="btn shadow-1 rounded-1 blue">Modifier le profil</a>
			<a href="#" class="btn shadow-1 rounded-1 blue">Modifier le mot de passe</a>
		</div>
		<div class="col-xs4">
	<h1>annonce</h1>
			{% for action in app.user.createdActions %}
	<h1>Titre:{{dump(action.title)}}</h1>
		
	{% endfor %}
			{# <h1>Annonce de {{creators.firstName}} {{app.user.lastName}}</div>
			</h1> #}
		
			{# {% for creators in creator %}
				
			{% if app.user.createdActions == creators.createdActions %}
			{% for actions in action %}
			<p></p>
			{% endfor %}
			<h1>baba</h1>
				{{action.title}}
			{% endif %}


			{% else %}
			oo
			{% endfor %} #}
			
				
			
			{# <p>{{dump(creators.createdActions)}}</p> #}
{# 
				{{dump(actions)}} #}
		
			{# {% for actions in app.user.actions %}
				
		<div class="col-xs1">Titre:</div>

		<div class="col-xs3">{{action.title}}</div>
			{% endfor %} #}

	</div>

{% endblock %}
